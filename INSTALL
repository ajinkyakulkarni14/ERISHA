INSTALLTION INSTRUCTION

Python version 3.6

Pre-requisites:

1. NVIDIA GPU + CUDA cuDNN

Install PyTorch 1.0 or above

Install Apex, (https://github.com/NVIDIA/apex)

Install python requirements: `pip install -r requirements.txt
